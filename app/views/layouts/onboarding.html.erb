<% content_for :content do %>
  <div class="max-w-3xl w-full mx-auto sm:px-6 lg:px-8 items-center py-4">
    <a href="#" class="mx-auto">
      <%= image_tag "yaht-logomark.svg", class: "mx-auto w-auto h-6", alt: "yaht - yet another habit tracker" %>
    </a>
  </div>
  <div class="flex flex-col items-start w-full flex-1">
    <div class="max-w-3xl w-full mx-auto px-4 my-4 md:my-8 flex-col md:flex-row relative md:mb-32 mb-16 sm:px-6 lg:px-8">
      <%= content_for?(:box_content) ? yield(:box_content) : yield %>
    </div>
  </div>
<% end %>
<%= render template: "layouts/application" %>